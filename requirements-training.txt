# Pole Position engineering/training dependencies (Python 3.11 x64 baseline).
#
# NVIDIA GPU workstation:
# Install the appropriate CUDA-enabled PyTorch build FIRST, then install this
# file. The version constraints below leave a compatible CUDA build in place --
# but only for a plain install. `pip install --upgrade` takes a directly named
# requirement to the newest version the index offers even when the installed
# one already satisfies the range, and PyPI's newest Windows wheel is CPU-only,
# so an upgrading install replaces the CUDA build. Pass a constraints file
# pinning the resolved torch and torchvision versions if you must use
# --upgrade; packaging/windows/build-local.ps1 does exactly that.
# Example for RTX 50-series / Blackwell:
#   python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# CPU-only systems may install this file directly.
torch>=2.2
torchvision>=0.17
ultralytics>=8.3,<9
onnx>=1.16,<2
onnxruntime>=1.18,<2
